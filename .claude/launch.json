{
    "version": "0.0.1",
    "configurations": [
        {
            "name": "forumify",
            "runtimeExecutable": "wsl.exe",
            "runtimeArgs": [
                "-d",
                "Ubuntu",
                "--",
                "bash",
                "-lc",
                "cd ~/dev/forumify && PATH=\"$HOME/.local/bin:$PATH\" PHP_CLI_SERVER_WORKERS=4 php -d upload_max_filesize=300M -d post_max_size=300M -S 0.0.0.0:8000 -t public"
            ],
            "port": 8000
        }
    ]
}
